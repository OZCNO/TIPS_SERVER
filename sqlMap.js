// crud
var sqlMap = {
    tax: {
        // 添加
        // add: 'insert into user(name, age) values (?, ?)',
        // 查询
        select: 'select * from tax_department'
    }
}

module.exports = sqlMap;